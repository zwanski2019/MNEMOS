# packages/schemas — shared types

Shared pydantic (Python) / zod (TS) type definitions for findings, scope decisions, agent runs,
and the events crossing gateway ↔ scanner ↔ analyst ↔ web.

Status: grows alongside P1–P5.
